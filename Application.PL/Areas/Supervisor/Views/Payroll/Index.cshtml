@{
    ViewData["Title"] = "Payroll";
}

<h2>Payroll</h2>

<p>This page provides UI for running payroll. Actual payroll logic is intentionally a stub.</p>

<form method="post" asp-action="RunPayroll">
    @Html.AntiForgeryToken()
    <button class="btn btn-primary">Run Payroll (stub)</button>
</form>

@if (TempData["Alert"] != null)
{
    <div class="alert alert-info mt-3">@TempData["Alert"]</div>
}
